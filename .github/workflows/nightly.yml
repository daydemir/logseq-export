name: Nightly Build

on:
  schedule: # Every 15 minutes
    - cron: '*/15 * * * *'

jobs:
  call-build-docker:
    uses: ./.github/workflows/build-docker.yml
    with:
      tag: nightly
      publish_tag: nightly
